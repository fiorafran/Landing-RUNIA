---
import Card from '../ui/Card.astro';
import Button from '../ui/Button.astro';

export interface Props {
  title?: string;
  subtitle?: string;
  solutions?: Array<{
    id: string;
    title: string;
    description: string;
    icon: string;
    color?: 'primary' | 'secondary' | 'accent' | 'success';
    features?: string[];
  }>;
  showCTA?: boolean;
  ctaText?: string;
  ctaLink?: string;
  className?: string;
}

const {
  title = "¿Qué soluciones ofrecemos?",
  subtitle = "Descubrí cómo nuestros chatbots con IA pueden transformar tu negocio y mejorar la experiencia de tus clientes",
  solutions = [
    {
      id: "automation",
      title: "Automatización 24/7",
      description: "Atención al cliente sin interrupciones, disponible todo el día y todos los días del año.",
      icon: "🤖",
      color: "primary",
      features: ["Respuestas instantáneas", "Sin límites de horario", "Escalabilidad automática"]
    },
    {
      id: "speed",
      title: "Respuesta Instantánea",
      description: "Respuestas inmediatas que mejoran la experiencia del cliente y aumentan la satisfacción.",
      icon: "⚡",
      color: "secondary",
      features: ["Tiempo de respuesta < 1s", "Múltiples conversaciones", "Contexto inteligente"]
    },
    {
      id: "costs",
      title: "Reducción de Costos",
      description: "Optimiza recursos y reduce gastos operativos significativamente mientras mejoras el servicio.",
      icon: "💰",
      color: "accent",
      features: ["Hasta 80% de ahorro", "Sin costos ocultos", "ROI medible"]
    },
    {
      id: "integration",
      title: "Integración Total",
      description: "Se integra perfectamente con WhatsApp Business y otros canales de comunicación.",
      icon: "🔗",
      color: "success",
      features: ["WhatsApp Business", "APIs personalizadas", "Sincronización en tiempo real"]
    }
  ],
  showCTA = true,
  ctaText = "Ver todas las soluciones",
  ctaLink = "#contact",
  className = ""
} = Astro.props;

const colorClasses = {
  primary: "bg-primary-100 text-primary-600",
  secondary: "bg-secondary-100 text-secondary-600",
  accent: "bg-accent-100 text-accent-600",
  success: "bg-green-100 text-green-600"
};

const gradientClasses = {
  primary: "from-primary-500 to-primary-600",
  secondary: "from-secondary-500 to-secondary-600",
  accent: "from-accent-500 to-accent-600",
  success: "from-green-500 to-green-600"
};
---

<section id="solutions" class={`section-padding bg-white ${className}`}>
  <div class="container-custom">
    <!-- Section Header -->
    <div class="text-center mb-20">
      <div class="inline-flex items-center px-4 py-2 bg-primary-100 text-primary-700 rounded-full text-sm font-medium mb-6">
        <span class="w-2 h-2 bg-primary-500 rounded-full mr-2 animate-pulse"></span>
        Soluciones Inteligentes
      </div>
      
      <h2 class="text-4xl md:text-5xl font-bold mb-6 text-neutral-900">
        {title}
      </h2>
      <p class="text-xl text-neutral-600 max-w-4xl mx-auto leading-relaxed">
        {subtitle}
      </p>
    </div>

    <!-- Solutions Grid -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8 mb-20">
      {solutions.map((solution, index) => (
        <div class="group relative">
          <!-- Background Gradient on Hover -->
          <div class="absolute inset-0 bg-gradient-to-br from-primary-500 to-secondary-500 rounded-2xl opacity-0 group-hover:opacity-100 transition-opacity duration-500 blur-xl scale-95 group-hover:scale-100"></div>
          
          <Card 
            padding="lg" 
            shadow="lg" 
            className="relative text-center group-hover:shadow-2xl transition-all duration-500 group-hover:-translate-y-2 bg-white/95 backdrop-blur-sm"
          >
            <!-- Icon with Enhanced Animation -->
            <div class="relative mb-8">
              <div class={`w-24 h-24 ${colorClasses[solution.color || 'primary']} rounded-3xl flex items-center justify-center mx-auto group-hover:scale-110 transition-transform duration-500 shadow-lg`}>
                <span class="text-4xl group-hover:scale-110 transition-transform duration-300">{solution.icon}</span>
              </div>
              
              <!-- Floating Elements -->
              <div class="absolute -top-2 -right-2 w-6 h-6 bg-gradient-to-br from-primary-400 to-primary-600 rounded-full opacity-0 group-hover:opacity-100 transition-opacity duration-500 animate-pulse"></div>
              <div class="absolute -bottom-2 -left-2 w-4 h-4 bg-gradient-to-br from-secondary-400 to-secondary-600 rounded-full opacity-0 group-hover:opacity-100 transition-opacity duration-500 delay-200 animate-pulse"></div>
            </div>
            
            <!-- Content with Enhanced Typography -->
            <h3 class="text-xl font-bold mb-4 text-neutral-900 group-hover:text-primary-600 transition-colors duration-300">
              {solution.title}
            </h3>
            
            <p class="text-neutral-600 leading-relaxed mb-6 group-hover:text-neutral-700 transition-colors duration-300">
              {solution.description}
            </p>

            <!-- Enhanced Features List -->
            {solution.features && solution.features.length > 0 && (
              <div class="mb-8">
                <ul class="text-left space-y-3">
                  {solution.features.map((feature, featureIndex) => (
                    <li class="flex items-center text-sm text-neutral-600 group-hover:text-neutral-700 transition-colors duration-300">
                      <div class="w-2 h-2 bg-gradient-to-r from-primary-500 to-secondary-500 rounded-full mr-3 flex-shrink-0 group-hover:scale-125 transition-transform duration-300"></div>
                      <span class="group-hover:translate-x-1 transition-transform duration-300">{feature}</span>
                    </li>
                  ))}
                </ul>
              </div>
            )}

            <!-- Enhanced Learn More Button -->
            <Button 
              variant="outline" 
              size="sm" 
              className="w-full group-hover:bg-gradient-to-r group-hover:from-primary-500 group-hover:to-secondary-500 group-hover:text-white group-hover:border-transparent transition-all duration-300 transform group-hover:scale-105"
            >
              <span class="mr-2">→</span>
              Saber más
            </Button>
          </Card>
        </div>
      ))}
    </div>

    <!-- Enhanced CTA Section -->
    {showCTA && (
      <div class="text-center">
        <div class="inline-flex items-center space-x-4">
          <Button 
            variant="secondary" 
            size="lg" 
            href={ctaLink}
            className="px-8 py-4 text-lg shadow-xl hover:shadow-2xl transform hover:-translate-y-1 transition-all duration-300"
          >
            <span class="mr-2">🚀</span>
            {ctaText}
          </Button>
          
          <div class="hidden md:flex items-center text-neutral-500">
            <span class="text-sm">o</span>
            <a href="#demo" class="ml-2 text-primary-600 hover:text-primary-700 font-medium transition-colors duration-300">
              probar demo gratis
            </a>
          </div>
        </div>
      </div>
    )}
  </div>

  <!-- Decorative Background Elements -->
  <div class="absolute top-1/4 left-10 w-32 h-32 bg-primary-100 rounded-full opacity-20 blur-3xl"></div>
  <div class="absolute bottom-1/4 right-10 w-40 h-40 bg-secondary-100 rounded-full opacity-20 blur-3xl"></div>
</section>

<style>
  @keyframes float {
    0%, 100% { transform: translateY(0px); }
    50% { transform: translateY(-10px); }
  }
  
  .animate-float {
    animation: float 3s ease-in-out infinite;
  }
</style> 