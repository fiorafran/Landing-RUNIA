---
import Card from '../ui/Card.astro';
import Button from '../ui/Button.astro';

export interface Props {
  title?: string;
  subtitle?: string;
  advantages?: Array<{
    id: string;
    title: string;
    description: string;
    icon: string;
    color?: 'primary' | 'secondary' | 'accent' | 'success' | 'warning';
    features?: string[];
    highlight?: string;
  }>;
  showDemo?: boolean;
  demoTitle?: string;
  demoDescription?: string;
  demoButtonText?: string;
  className?: string;
}

const {
  title = "¿Por qué elegirnos?",
  subtitle = "Nuestras ventajas competitivas que nos diferencian de la competencia y garantizan tu éxito",
  advantages = [
    {
      id: "transparency",
      title: "Precios Transparentes",
      description: "Sin costos ocultos ni sorpresas. Sabés exactamente qué pagás desde el primer momento.",
      icon: "💰",
      color: "primary",
      features: ["Sin costos ocultos", "Precios fijos", "Sin comisiones sorpresa"],
      highlight: "Precios claros desde el día 1"
    },
    {
      id: "unlimited",
      title: "Usuarios Ilimitados",
      description: "No hay límites en la cantidad de usuarios que pueden interactuar con tu chatbot.",
      icon: "∞",
      color: "secondary",
      features: ["Sin límites de usuarios", "Escalabilidad automática", "Sin costos por uso"],
      highlight: "Escala sin límites"
    },
    {
      id: "speed",
      title: "Configuración Rápida",
      description: "Tu chatbot estará funcionando en menos de 5 minutos, sin complicaciones técnicas.",
      icon: "⚡",
      color: "accent",
      features: ["Setup en 5 minutos", "Sin conocimientos técnicos", "Configuración guiada"],
      highlight: "Funcionando en 5 minutos"
    },
    {
      id: "support",
      title: "Soporte Personalizado",
      description: "Equipo de expertos disponible para ayudarte en cada paso del proceso.",
      icon: "🎯",
      color: "success",
      features: ["Soporte 24/7", "Expertos dedicados", "Respuesta en menos de 1 hora"],
      highlight: "Soporte experto 24/7"
    },
    {
      id: "security",
      title: "Seguridad Garantizada",
      description: "Tus datos están protegidos con los más altos estándares de seguridad y privacidad.",
      icon: "🔒",
      color: "warning",
      features: ["Encriptación end-to-end", "Cumplimiento GDPR", "Backups automáticos"],
      highlight: "Seguridad de nivel bancario"
    },
    {
      id: "roi",
      title: "ROI Garantizado",
      description: "Nuestros clientes ven retorno de inversión en el primer mes de implementación.",
      icon: "📈",
      color: "primary",
      features: ["ROI en 30 días", "Métricas detalladas", "Optimización continua"],
      highlight: "ROI garantizado en 30 días"
    }
  ],
  showDemo = true,
  demoTitle = "Probá nuestro chatbot en acción",
  demoDescription = "Descubrí cómo puede transformar tu negocio con una demostración personalizada",
  demoButtonText = "Probar demo gratis",
  className = ""
} = Astro.props;

const colorClasses = {
  primary: "bg-primary-100 text-primary-600",
  secondary: "bg-secondary-100 text-secondary-600",
  accent: "bg-accent-100 text-accent-600",
  success: "bg-green-100 text-green-600",
  warning: "bg-yellow-100 text-yellow-600"
};

const gradientClasses = {
  primary: "from-primary-500 to-primary-600",
  secondary: "from-secondary-500 to-secondary-600",
  accent: "from-accent-500 to-accent-600",
  success: "from-green-500 to-green-600",
  warning: "from-yellow-500 to-yellow-600"
};
---

<section id="why-choose-us" class={`section-padding bg-gradient-to-br from-neutral-50 to-neutral-100 relative overflow-hidden ${className}`}>
  <!-- Background Pattern -->
  <div class="absolute inset-0 bg-grid-pattern opacity-5"></div>
  
  <div class="container-custom relative z-10">
    <!-- Section Header -->
    <div class="text-center mb-20">
      <div class="inline-flex items-center px-6 py-3 bg-gradient-to-r from-primary-500 to-secondary-500 text-white rounded-full text-sm font-medium mb-8 shadow-lg">
        <span class="w-2 h-2 bg-white rounded-full mr-3 animate-pulse"></span>
        Ventajas Competitivas
        <span class="ml-3 text-xs opacity-90">6 razones para elegirnos</span>
      </div>
      
      <h2 class="text-4xl md:text-6xl font-bold mb-8 text-neutral-900">
        {title}
      </h2>
      <p class="text-xl text-neutral-600 max-w-4xl mx-auto leading-relaxed">
        {subtitle}
      </p>
    </div>

    <!-- Advantages Grid -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 mb-20">
      {advantages.map((advantage, index) => (
        <div class="group relative" style={`animation-delay: ${index * 100}ms`}>
          <!-- Background Gradient on Hover -->
          <div class="absolute inset-0 bg-gradient-to-br from-primary-500 to-secondary-500 rounded-3xl opacity-0 group-hover:opacity-100 transition-opacity duration-700 blur-xl scale-95 group-hover:scale-100"></div>
          
          <Card 
            padding="xl" 
            shadow="xl" 
            className="relative group-hover:shadow-2xl transition-all duration-700 hover:-translate-y-3 bg-white/95 backdrop-blur-sm border border-white/20"
          >
            <!-- Icon with Enhanced Animation -->
            <div class="relative mb-8">
              <div class={`w-20 h-20 ${colorClasses[advantage.color || 'primary']} rounded-3xl flex items-center justify-center mx-auto group-hover:scale-110 transition-transform duration-700 shadow-xl group-hover:shadow-2xl`}>
                <span class="text-3xl group-hover:scale-110 transition-transform duration-500 group-hover:rotate-12">{advantage.icon}</span>
              </div>
              
              <!-- Floating Elements -->
              <div class="absolute -top-2 -right-2 w-6 h-6 bg-gradient-to-br from-primary-400 to-primary-600 rounded-full opacity-0 group-hover:opacity-100 transition-opacity duration-700 animate-pulse"></div>
              <div class="absolute -bottom-2 -left-2 w-4 h-4 bg-gradient-to-br from-secondary-400 to-secondary-600 rounded-full opacity-0 group-hover:opacity-100 transition-opacity duration-700 delay-300 animate-pulse"></div>
              <div class="absolute top-1/2 -right-4 w-3 h-3 bg-gradient-to-br from-accent-400 to-accent-600 rounded-full opacity-0 group-hover:opacity-100 transition-opacity duration-700 delay-500 animate-pulse"></div>
            </div>
            
            <!-- Content -->
            <h3 class="text-2xl font-bold mb-4 text-neutral-900 group-hover:text-primary-600 transition-colors duration-500">
              {advantage.title}
            </h3>
            
            <p class="text-neutral-600 leading-relaxed mb-6 group-hover:text-neutral-700 transition-colors duration-500">
              {advantage.description}
            </p>

            <!-- Enhanced Highlight -->
            {advantage.highlight && (
              <div class="bg-gradient-to-r from-primary-50 via-secondary-50 to-accent-50 border-l-4 border-primary-500 p-5 mb-6 rounded-r-xl shadow-sm group-hover:shadow-md transition-shadow duration-500">
                <div class="text-sm font-bold text-primary-700 flex items-center">
                  <span class="mr-2">💡</span>
                  {advantage.highlight}
                </div>
              </div>
            )}

            <!-- Enhanced Features List -->
            {advantage.features && advantage.features.length > 0 && (
              <div class="mb-8">
                <ul class="space-y-3">
                  {advantage.features.map((feature, featureIndex) => (
                    <li class="flex items-center text-sm text-neutral-600 group-hover:text-neutral-700 transition-colors duration-500" style={`animation-delay: ${featureIndex * 50}ms`}>
                      <div class="w-2 h-2 bg-gradient-to-r from-primary-500 to-secondary-500 rounded-full mr-3 flex-shrink-0 group-hover:scale-150 transition-transform duration-500"></div>
                      <span class="group-hover:translate-x-2 transition-transform duration-500">{feature}</span>
                    </li>
                  ))}
                </ul>
              </div>
            )}

            <!-- Enhanced Learn More Button -->
            <Button 
              variant="outline" 
              size="md" 
              className="w-full group-hover:bg-gradient-to-r group-hover:from-primary-500 group-hover:to-secondary-500 group-hover:text-white group-hover:border-transparent transition-all duration-500 transform group-hover:scale-105 shadow-lg group-hover:shadow-xl"
            >
              <span class="mr-2 group-hover:rotate-45 transition-transform duration-500">→</span>
              Saber más
            </Button>
          </Card>
        </div>
      ))}
    </div>

    <!-- Enhanced Demo Section -->
    {showDemo && (
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-16 items-center">
        <div class="space-y-10">
          <div class="text-center lg:text-left">
            <h3 class="text-4xl font-bold mb-6 text-neutral-900">
              {demoTitle}
            </h3>
            <p class="text-xl text-neutral-600 leading-relaxed">
              {demoDescription}
            </p>
          </div>

          <div class="space-y-8">
            <div class="flex items-center space-x-6 group">
              <div class="w-16 h-16 bg-gradient-to-br from-green-400 to-green-600 rounded-2xl flex items-center justify-center shadow-lg group-hover:shadow-xl transition-shadow duration-500 group-hover:scale-110 transition-transform duration-500">
                <span class="text-white text-2xl">✓</span>
              </div>
              <div>
                <div class="text-xl font-bold text-neutral-900 group-hover:text-primary-600 transition-colors duration-500">Demo personalizada</div>
                <div class="text-lg text-neutral-600">Adaptada a tu industria y necesidades específicas</div>
              </div>
            </div>

            <div class="flex items-center space-x-6 group">
              <div class="w-16 h-16 bg-gradient-to-br from-blue-400 to-blue-600 rounded-2xl flex items-center justify-center shadow-lg group-hover:shadow-xl transition-shadow duration-500 group-hover:scale-110 transition-transform duration-500">
                <span class="text-white text-2xl">⚡</span>
              </div>
              <div>
                <div class="text-xl font-bold text-neutral-900 group-hover:text-primary-600 transition-colors duration-500">Sin compromiso</div>
                <div class="text-lg text-neutral-600">Probá gratis sin obligación de compra</div>
              </div>
            </div>

            <div class="flex items-center space-x-6 group">
              <div class="w-16 h-16 bg-gradient-to-br from-purple-400 to-purple-600 rounded-2xl flex items-center justify-center shadow-lg group-hover:shadow-xl transition-shadow duration-500 group-hover:scale-110 transition-transform duration-500">
                <span class="text-white text-2xl">🎯</span>
              </div>
              <div>
                <div class="text-xl font-bold text-neutral-900 group-hover:text-primary-600 transition-colors duration-500">Resultados inmediatos</div>
                <div class="text-lg text-neutral-600">Vé los beneficios desde el primer día</div>
              </div>
            </div>
          </div>

          <div class="text-center lg:text-left">
            <Button variant="primary" size="lg" className="px-10 py-5 text-xl shadow-xl hover:shadow-2xl transform hover:-translate-y-1 transition-all duration-500">
              <span class="mr-3">🚀</span>
              {demoButtonText}
            </Button>
          </div>
        </div>

        <div class="bg-white rounded-3xl p-10 shadow-2xl border border-white/20 backdrop-blur-sm">
          <div class="text-center">
            <div class="w-32 h-32 bg-gradient-to-br from-primary-500 to-secondary-500 rounded-full flex items-center justify-center mx-auto mb-8 shadow-2xl">
              <span class="text-white text-6xl">🤖</span>
            </div>
            <h4 class="text-3xl font-bold mb-6 text-neutral-900">
              Chatbot Demo
            </h4>
            <p class="text-lg text-neutral-600 mb-8 leading-relaxed">
              Interactuá con nuestro chatbot y descubrí cómo puede manejar las consultas de tus clientes de manera inteligente
            </p>
            
            <!-- Enhanced Demo Chat Interface -->
            <div class="bg-gradient-to-br from-neutral-50 to-neutral-100 rounded-2xl p-6 mb-8 border border-neutral-200">
              <div class="flex items-center space-x-4 mb-4">
                <div class="w-10 h-10 bg-gradient-to-br from-primary-500 to-secondary-500 rounded-full flex items-center justify-center shadow-lg">
                  <span class="text-white text-lg">🤖</span>
                </div>
                <div class="text-lg font-semibold text-neutral-700">Chatbot IA</div>
                <div class="flex items-center space-x-1">
                  <div class="w-2 h-2 bg-green-500 rounded-full animate-pulse"></div>
                  <span class="text-sm text-green-600 font-medium">En línea</span>
                </div>
              </div>
              <div class="bg-white rounded-xl p-4 text-base text-neutral-600 shadow-sm border border-neutral-100">
                ¡Hola! Soy tu asistente virtual. ¿En qué puedo ayudarte hoy? Puedo responder preguntas, agendar citas, o ayudarte con cualquier consulta.
              </div>
            </div>

            <Button variant="secondary" size="lg" className="w-full py-4 text-lg shadow-lg hover:shadow-xl transform hover:-translate-y-1 transition-all duration-500">
              <span class="mr-2">💬</span>
              Iniciar conversación
            </Button>
          </div>
        </div>
      </div>
    )}
  </div>

  <!-- Enhanced Decorative Elements -->
  <div class="absolute top-1/4 left-10 w-40 h-40 bg-gradient-to-br from-primary-400 to-primary-600 rounded-full opacity-10 blur-3xl animate-float"></div>
  <div class="absolute bottom-1/4 right-10 w-48 h-48 bg-gradient-to-br from-secondary-400 to-secondary-600 rounded-full opacity-10 blur-3xl animate-float-delayed"></div>
  <div class="absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 w-64 h-64 bg-gradient-to-br from-accent-400 to-accent-600 rounded-full opacity-5 blur-3xl animate-float-slow"></div>
</section>

<style>
  .bg-grid-pattern {
    background-image: 
      linear-gradient(rgba(0,0,0,0.1) 1px, transparent 1px),
      linear-gradient(90deg, rgba(0,0,0,0.1) 1px, transparent 1px);
    background-size: 50px 50px;
  }
  
  @keyframes float {
    0%, 100% { transform: translateY(0px) rotate(0deg); }
    50% { transform: translateY(-20px) rotate(180deg); }
  }
  
  @keyframes float-delayed {
    0%, 100% { transform: translateY(0px) rotate(0deg); }
    50% { transform: translateY(-15px) rotate(-180deg); }
  }
  
  @keyframes float-slow {
    0%, 100% { transform: translateY(0px) scale(1); }
    50% { transform: translateY(-10px) scale(1.1); }
  }
  
  .animate-float {
    animation: float 6s ease-in-out infinite;
  }
  
  .animate-float-delayed {
    animation: float-delayed 8s ease-in-out infinite;
  }
  
  .animate-float-slow {
    animation: float-slow 10s ease-in-out infinite;
  }
</style> 